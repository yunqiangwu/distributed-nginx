{
  "name": "distributed-nginx",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test:2222": "NGINX_DIST=test/dist1 node index.js",
    "build": "docker build . -t distributed-nginx:1",
    "test": "npm run build && lerna exec -- ./test.sh"
  },
  "dependencies": {
    "redis": "^4.0.1"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
