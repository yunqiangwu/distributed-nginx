{
  "name": "distributed-nginx",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "NODE_DEBUG=true node index.js",
    "build": "docker build . -t jonneywu/distributed-nginx:1",
    "test": "npm run build && lerna exec -- . ./test.sh"
  },
  "dependencies": {
    "multicast-dns": "^7.2.4",
    "redis": "^4.0.1"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
